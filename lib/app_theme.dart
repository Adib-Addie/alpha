import 'package:flutter/material.dart';

/// Shared dark theme palette used across Alpha Graphics screens.
class AppTheme {
  AppTheme._();

  // Background gradient colors
  static const Color backgroundStart = Color(0xFF1A1A2E);
  static const Color backgroundMid = Color(0xFF16213E);
  static const Color backgroundEnd = Color(0xFF0F3460);
  static const Color surfaceElevated = Color(0xFF233554);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB8B8CC);
  static const Color textMuted = Color(0xFF6B7280);

  // Borders & dividers
  static const Color divider = Color(0xFF2A3042);
  static const Color border = Color(0xFF3D4460);
}
